# G-01 — DONE
Updated: Finished implementation, verification, and the final report; only the cross-owned obsolete drawer test remains for its owner.
Done:
- Confirmed G-01 path ownership and frozen `Screen` API.
- Confirmed stack-only navigation, safe-area, gesture, and identity-binding requirements.
- Inventoried the current route tree, drawer implementation, and owned tests.
- Added `Screen` with native-header-aware top insets, standard gutters, bottom safety, keyboard-aware scrolling, and a sticky footer.
- Added tests for single top-inset ownership and keyboard-dependent footer positioning.
- Deleted the drawer navigator/content/tests and removed `@react-navigation/drawer` from `package.json`.
- Added a single protected native stack, stable route map, direct header destinations, and full-screen horizontal back gestures.
- Wrapped ordinary protected routes and onboarding states with `Screen`; removed onboarding's duplicated keyboard strategies.
- Added synchronous `me.user.id` binding in AuthGate with logout/401/server/account-switch clearing and protected-runtime gating.
- Added route-map, direct-navigation, gesture, and identity-ordering tests.
- Kept keyboard-controller bindings lazy for ordinary route imports so non-device unit tests remain isolated.
- Passed all G-01 focused tests and all non-stale full-suite tests; full lint is clean.
Now: Complete; handing off through `docs/native/reports/G-01.md`.
Next:
- Cross-owned app-shell test update and package-lock reconciliation by their owners.
- Feature-owned removal of internal duplicate safe-area/custom-header consumers.
Blocked: `mobile/src/lib/__tests__/app-shell.test.tsx` is outside G-01 ownership and still asserts the deleted drawer contract; it causes the only full-suite/typecheck failure.
Files written: docs/native/progress/G-01.md; mobile/package.json; mobile/src/app/** (G-01 route/layout updates and tests); mobile/src/components/layout/**; mobile/src/components/nav/**; mobile/src/lib/auth-gate.tsx
