# R4-08 Progress

- Status: complete; ready for integration
- Just finished: full verification and the final R4-08 report
- Working on: none
- Next: integration owner applies the recorded `AppHeader` accessory/actions composition follow-up, then the separate iPhone pass verifies physical gesture arbitration
- Blockers: `AppHeader` currently renders `accessory` instead of `actions` when both are supplied; R4-08 needs both the connection badge and terminal menu, so this requires an R4-01 follow-up without changing the frozen prop shape
- Files written: terminal route/header/overlay/chrome sources and tests, `docs/native/progress/R4-08.md`, `docs/native/reports/R4-08.md`
