# G-06 — DONE
Updated: Completed implementation, focused verification, exact full-tree snapshots, and the final report.
Done:
- Confirmed owned paths and no-git/no-install/no-device constraints
- Confirmed native-stack header, `NativePopover`, and guarded glass contracts
- Removed the folder path and custom close chevron from the terminal header
- Moved terminal identity, aligned status, and actions into the native-stack header
- Replaced `Menu` with all eight actions in `NativePopover`; Kill session is destructive
- Removed connected-state `SwipeDismissOverlay`/`FullSurfaceDismiss` wrappers
- Enabled native full-screen horizontal back for loading, error, and connected route states
- Disabled WKWebView back/forward navigation gestures so the parent stack owns horizontal back
- Added header, popover, per-state dismissal, axis-lock, and WebView arbitration assertions
- Passed `npm run typecheck` and `npm run lint` before concurrent G-05 replacement; G-06-owned checks remain clean
- Passed all focused terminal tests: 14 suites, 62 tests
Now: Done; handing the G-06 report to the orchestrator.
Next:
- Orchestrator/G-05 reruns full-tree verification after workspace-detail replacement settles
Blocked: none for G-06-owned paths; exact full-tree commands currently report only concurrent G-05 workspace-detail diagnostics/failures.
Files written: mobile/src/app/terminal/[sessionId].tsx; mobile/src/app/terminal/__tests__/terminal-screen.test.tsx; mobile/src/components/terminal-ui/terminal-header.tsx; mobile/src/components/terminal-ui/terminal-overlay.tsx; mobile/src/components/terminal-ui/__tests__/terminal-header.test.tsx; mobile/src/components/terminal-ui/__tests__/terminal-overlay.test.tsx; mobile/src/terminal/TerminalSurface.tsx; mobile/src/terminal/__tests__/TerminalSurface.test.tsx; docs/native/progress/G-06.md
