/** An inline link in the grimoire frame: ember with a quiet underline, hellfire on hover. */
export const LINK =
  "text-ember underline decoration-ember/40 underline-offset-4 transition-colors hover:text-hellfire hover:decoration-hellfire";

/** The install one-liner as the static pages print it (the landing page derives its own from the origin). */
export const INSTALL_COMMAND = "curl -fsSL https://spawnd.dev/install.sh | sh";
