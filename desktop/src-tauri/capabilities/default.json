{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The one window while it shows the bundled wizard. The product — a remote page in the same window — matches no capability and so has no IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "opener:default",
    "process:default",
    "updater:default"
  ]
}

